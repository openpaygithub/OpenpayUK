const path = require('./path')
module.exports = works = {
    label: "How it works",
    url: `${path}works.html`,
    clickSelector: ".cookie-popup__button",
    selectors: ["document"],
    misMatchThreshold: 0.1
}